import React from 'react';
export const metadata = {
    title:"install application"
}

const InstallationPage = () => {
    return (
        <div>
            <h1>hi this is installation page</h1>
        </div>
    );
};

export default InstallationPage;